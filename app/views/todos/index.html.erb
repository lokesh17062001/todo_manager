<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="reset.css">
  <link rel="stylesheet" href="todo-app.css">
  <title>Todo-Manager-Application</title>
</head>

<body>
  <main class="TodoPageHead">
    <article>
      <h1 class=TodoPage-Title>My To-do List</h1>
      <%= form_with(url: "/todos", method: "post",class: "AddTodo") do %>
        <%= text_field_tag(
           name='todo_text',
           value=nil,
           class:"AddTodo-Text",
           placeholder: "what's next?")%>
         <%= date_field_tag(
           name='due_date',
           value=nil,
           class:"AddTodo-Date")%>
         <%= button_tag(
           value='Add',
           class: "AddTodo-Button") %>
      <% end %>

      <%= render partial:"todo-section", locals: {
       todo_title:"Overdue",
       todos:@todos.overdue,
       show_due_date: true
      } %>
      <%= render partial:"todo-section", locals: {
       todo_title:"Due Today",
       todos:@todos.due_today,
      show_due_date: false
      } %>
      <%=render partial:"todo-section", locals: {
      todo_title:"Due Later",
      todos:@todos.due_later,
      show_due_date: true
     } %>

      </body>

</html>
